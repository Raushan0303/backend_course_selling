/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['your-domain-here.com'], // Add the domain where your background image is hosted
  },
}

module.exports = nextConfig